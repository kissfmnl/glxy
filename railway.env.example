# Kopieer dit bestand naar `.env.railway` (één map met je glxy-project).
#
# Gebruik DATABASE_PUBLIC_URL (niet de interne DATABASE_URL met postgres.railway.internal —
# die werkt niet vanaf je Mac).
#
# Plak daarin DATABASE_URL = Railway → Postgres-plugin → DATABASE_PUBLIC_URL
# Voor je eigen Mac vaak nodig: voeg aan het eind van de URL toe: ?sslmode=require
#
# Voorbeeld (gebruik jouw echte URL uit Railway):
# DATABASE_URL="postgresql://postgres:XXXX@turntable.proxy.rlwy.net:12345/railway?sslmode=require"
#
# Daarna lokaal in Terminal:  npm run db:push:env
#
# .env.railway staat in .gitignore — wordt nooit naar GitHub gezet.

DATABASE_URL=""
