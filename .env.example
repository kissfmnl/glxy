# ===========================================================
# GLXY Radio — omgeving (statische frontend-build)
# ===========================================================
# Kopieer naar .env.local alleen als je iets wilt overschrijven.
# Commit .env.local nooit naar Git.

# Optioneel: toegangscode voor /monitor (in code staat ook een fallback)
# MONITOR_PAGE_CODE=kies_zelf_iets_veiligs

# Geen DATABASE_URL nodig voor deze tak — backend is verwijderd uit de app.
