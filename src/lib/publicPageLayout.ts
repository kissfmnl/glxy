/** Shared outer shell for public pages — same max width and horizontal rhythm as DJ’s / frequenties. */
export const PUBLIC_PAGE_SHELL =
  "mx-auto w-full max-w-6xl min-w-0 px-3.5 sm:px-4 md:px-8 py-10 overflow-x-hidden";

/** Breed zoals homepage / zenderstrip (1500px cap). */
export const PUBLIC_PAGE_SHELL_WIDE =
  "mx-auto w-full max-w-[1500px] min-w-0 px-4 sm:px-6 md:px-8 py-10 overflow-x-hidden";

/**
 * Nudge page titles / intros to align with text inside rounded cards (cards use p-4 md:p-6).
 */
export const PUBLIC_PAGE_INTRO = "md:pl-5 lg:pl-6";
