/** Eerste regel (kleine caps) in kaarten op de wave-homepage en vergelijkbare panels. */
export const KISS_PANEL_TITLE = "text-[10px] font-black uppercase tracking-[0.2em] text-gray-500 leading-tight";

/** Zelfde typografie op donkere panelen (concerten). */
export const KISS_PANEL_TITLE_ON_DARK = "text-[10px] font-black uppercase tracking-[0.2em] text-white/70 leading-tight";

/** Bovenste padding + horizontale inset voor paneltitels (gelijk aan p-5 content). */
export const KISS_PANEL_HEADER_BOX = "px-5 pt-5 pb-3";

/** Eerste inhoud onder de titelregel — overal dezelfde verticale sprong. */
export const KISS_PANEL_HEADER_GAP = "mt-5";

/** Standaard horizontale + onder-padding voor panelbody’s. */
export const KISS_PANEL_BODY_PAD = "px-5 pb-5";
